<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rose.tiketku">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditProfile"></activity>
        <activity android:name=".MyTicket" />
        <activity android:name=".Activities.ProfileActivity" />
        <activity android:name=".Activities.SuccessBuyTicketActivity" />
        <activity android:name=".Activities.TicketCheckoutActivity" />
        <activity android:name=".Activities.TicketDetailActivity" />
        <activity android:name=".Activities.HomeActivity" />
        <activity android:name=".Activities.SuccessRegisterActivity" />
        <activity android:name=".Activities.RegisterTwoActivity" />
        <activity android:name=".Activities.RegisterOneActivity" />
        <activity android:name=".Activities.SignInActivity" />
        <activity android:name=".Activities.GetStartedActivity" />
        <activity android:name=".Activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>